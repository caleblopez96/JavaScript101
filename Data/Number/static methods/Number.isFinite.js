// you can use Number.isFinite() to determine if a number passed 
// is a finite value

// SYNTAX:
// Number.isFinite(value)

// EXAMPLE:
console.log(Number.isFinite(1 / 0)) // false
console.log(Number.isFinite(2 / 10)) // true