// use Number.isInteger() to determine if number is integer

// SYNTAX:
// Number.isInteger(value)

// EXAMPLE:
console.log(Number.isInteger(3)) // true
console.log(Number.isInteger('a')) // false