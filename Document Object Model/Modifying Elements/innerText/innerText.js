// use the .innerText property to get or set the text content of an HTML element.

// EXAMPLE: Retrieve textContent of an element
const element = document.querySelector('.element');
const textContent = element.innerText;

// EXAMPLE: Set textContent of an element
element.innerText = '';
