// use .textContent to get or set the text content of an HTML element.

// EXAMPLE: retrieving text content of an element
const element = document.querySelector('.element');
const textContent = element.textContent;

// EXAMPLE: setting the textContent of an element.
element.textContent = ' ';