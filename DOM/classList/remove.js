// use classList.remove() to remove one or more classes to an element. 

// EXAMPLE:
const element = document.querySelector('element');
element.classList.add('active', 'highlight');
element.classList.remove('active');