// used to compare two values or expressions to check if the left operand is less than the right operand. 

// EXAMPLE: 
3 < 5;   // true, because 3 is less than 5
5 < 3;   // false, because 5 is not less than 3
5 < 5;   // false, because 5 is not less than 5 (they are equal)