// Unary Plus (+): Converts its operand into a number, or returns NaN if the operand cannot be converted.

let num = +"42"; // num will be 42 (converted to a number)

const x = 1;
const y = -1;

console.log(+x);
// Expected output: 1

console.log(+y);
// Expected output: -1