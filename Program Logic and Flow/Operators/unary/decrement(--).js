// Increment (--) : decreases value of a variable by 1.

function decreaseOperator() {
    let num = 5;
    if(num <= 5) {
        num--
        return num;
    }
}

let result = decreaseOperator();
console.log(result); // 4