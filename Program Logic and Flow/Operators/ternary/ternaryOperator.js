// a more concise way to crite conditional statements.

// SYNTAX:
condition ? expressionIfTrue : expressionIfFalse;

// EXAMPLE:

const age = 20;
const isAdult = age >= 18 ? "Yes" : "No";

// EXAMPLE:
let count = 0;
const plural = count === 1 ? "Car" : "Cars";
