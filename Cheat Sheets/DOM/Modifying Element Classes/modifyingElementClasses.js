element.classList // access elements classes
element.classList.add("className") // add class to element
element.classList.remove("className") // remove class from element
element.classList.toggle("className") // toggle on/off a class
element.classList.contains("className") // see if element contains class