// use Number.isNan() to determine if a value is not a number

// SYNTAX:
// Number.isNaN(value)

// PARAMETERS:
// value - value to check if not a number

// EXAMPLE:
console.log(Number.isNaN(2)); // false
console.log(Number.isNaN(NaN)); // true

console.log(isNaN(0 / 0));
