// generate random number 1 - max
const getRandomNumber = (max) => Math.floor(Math.random() * max);
console.log(getRandomNumber(100));

// generate random number 1 - 10
const getRandomNumberOneThroughTen = () => Math.floor(Math.random() * 10);
console.log(getRandomNumberOneThroughTen());
