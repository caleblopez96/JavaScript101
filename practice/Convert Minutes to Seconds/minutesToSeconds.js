// Convert Minutes to Seconds
// Write a function that converts a number of minutes into seconds.For example, if 5 is passed in, the function should return 300.

function minutesToSeconds(minutes) {
  return minutes * 60;
}

minutesToSeconds(5); // 300
