// Increment (++) : increases value of a variable by 1.

function incrementOperator() {
    let num = 5;
    if(num <= 5) {
        num++
        return num;
    }
}

let result = incrementOperator();
console.log(result); // 6